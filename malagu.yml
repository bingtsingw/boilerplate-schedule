mode: [timer]
targets:
  - backend

malagu:
  cloud:
    disableProjectId: true
    trigger:
      triggerConfig:
        payload:
        cronExpression: 0 0 */1 * * *
    function:
      instanceConcurrency: 100
      memorySize: 128
      timeout: 3600
      name: boilerplate-schedule
