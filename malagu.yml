mode: [timer]
targets:
  - backend

malagu:
  cloud:
    disableProjectId: true
    trigger:
      name: timer
    function:
      instanceConcurrency: 100
      memorySize: 128
      timeout: 3600
      name: boilerplate-schedule
